var docTitle = document.title;

var targetTitle = '(JPEG Image, 260\u00a0\u00D7\u00a0260 pixels)';

/* quick code for testing the document.title string in case FF changes something again :( */
/*
for (var i=0;i<docTitle.length;i++)
{
	console.log( i + " " + docTitle.charAt(i) + " vs " + targetTitle.charAt(i) );

	if( docTitle.charAt(i) != targetTitle.charAt(i) ){
		console.log("bad");
	}
}
*/

if( docTitle === targetTitle ) {
    window.location.href = 'http://exhentai.org/login';
}